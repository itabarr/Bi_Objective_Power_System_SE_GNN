# install uv on mac
brew install uv

# install Virtual Env
uv venv --python 3.12 .venv

# activate
source .venv/bin/activate

# uv installation
uv add pandas numpy pandapower torchmetrics
uv pip install torch torchvision torchaudio
uv pip install torch-geometric -f https://data.pyg.org/whl/torch-$(python -c "import torch; print(torch.__version__.split('+')[0])")+cpu.html